{
  "table_name": "roles",
  "indexes": {
    "primary": {
      "name": "PRIMARY",
      "type": "PRIMARY",
      "columns": ["id"],
      "description": "Primary key index"
    },
    "unique_indexes": [
      {
        "name": "uk_roles_name",
        "type": "UNIQUE",
        "columns": ["name"],
        "description": "Index untuk memastikan nama role unik"
      }
    ],
    "single_column_indexes": [
      {
        "name": "idx_roles_level",
        "type": "INDEX",
        "columns": ["level"],
        "description": "Index untuk query berdasarkan level role hierarchy"
      },
      {
        "name": "idx_roles_is_active",
        "type": "INDEX",
        "columns": ["is_active"],
        "description": "Index untuk filter active roles"
      },
      {
        "name": "idx_roles_is_system",
        "type": "INDEX",
        "columns": ["is_system"],
        "description": "Index untuk filter system roles"
      },
      {
        "name": "idx_roles_created_by",
        "type": "INDEX",
        "columns": ["created_by"],
        "description": "Index untuk join ke users table"
      }
    ],
    "composite_indexes": [
      {
        "name": "idx_roles_active_level",
        "type": "INDEX",
        "columns": ["is_active", "level"],
        "description": "Index untuk query active roles dengan sorting by level"
      },
      {
        "name": "idx_roles_system_active",
        "type": "INDEX",
        "columns": ["is_system", "is_active"],
        "description": "Index untuk query system active roles"
      },
      {
        "name": "idx_roles_level_name",
        "type": "INDEX",
        "columns": ["level", "name"],
        "description": "Index untuk query roles by level dengan name"
      }
    ],
    "performance_indexes": [
      {
        "name": "idx_roles_hierarchy_query",
        "type": "INDEX",
        "columns": ["level", "is_active", "is_system"],
        "description": "Optimized index untuk role hierarchy queries"
      }
    ]
  },
  "index_analysis": {
    "query_patterns": [
      {
        "query": "SELECT * FROM roles WHERE is_active = true ORDER BY level DESC",
        "used_indexes": ["idx_roles_active_level"],
        "optimization": "Efisien untuk role hierarchy display"
      },
      {
        "query": "SELECT * FROM roles WHERE name = ? AND is_active = true",
        "used_indexes": ["uk_roles_name", "idx_roles_is_active"],
        "optimization": "Cukup optimal untuk role lookup"
      },
      {
        "query": "SELECT * FROM roles WHERE is_system = false AND is_active = true",
        "used_indexes": ["idx_roles_system_active"],
        "optimization": "Efisien untuk custom role listing"
      }
    ]
  },
  "maintenance": {
    "rebuild_frequency": "quarterly",
    "monitoring": {
      "unused_indexes": [
        "idx_roles_created_by"
      ],
      "high_usage_indexes": [
        "uk_roles_name",
        "idx_roles_level",
        "idx_roles_is_active"
      ]
    }
  }
}